import React, { Component } from 'react';

const Ccomponent = (props) => {
    console.log(props); 
    return (
        <div>
            <h1>I am {props.myname} and my message : {props.message}</h1>
        </div>
    );
    
}
 
export 

default Ccomponent;